# Validation record

Local development and production compilation are validated separately from live AWS execution.

- Domain/API tests exercise ranking, energy/time/urgency, dependency DAGs, completion, skipping, postponement, duration calibration, idempotency, revision conflicts, completed-work preservation, adaptation, malformed AI retry, production fallback rejection, origin checks and user isolation.
- Browser acceptance covers actual forms and HTTP persistence: account → goal → editable plan → low-energy recommendation → completion → high-energy recommendation → blocker skip → accepted prerequisite → postponement → progress → signout/signin persistence. Anonymous routes are also checked.
- Desktop and mobile screenshot artifacts are generated by Playwright in `test-results/` (ignored). Mobile overflow is asserted after the actual Today controls load.
- Production build and CDK synthesis are run locally. Synthesis is not deployment.
- AWS credentials are unavailable. Cloud provisioning, live Cognito and Bedrock responses, and Amplify runtime smoke testing are not claimed as verified.

Final results: 19/19 domain/API tests and 2/2 Chromium acceptance tests passed on Next.js 15.5.25. TypeScript and ESLint pass without warnings. Production build and CDK synthesis succeed. npm audit reports zero vulnerabilities. The acceptance flow also verifies Escape-to-close dialogs and loaded Today layouts at 820 px and 390 px widths.
